package view

import net.objecthunter.exp4j.Expression
import javax.swing.JFrame

class PlotView(points: Array<DoubleArray>, expression1: Expression, expression2: Expression, parent: JFrame) {
    val frame = JFrame()

    /*
    Find highest and lowest x point,
    Find highest and lowest y point,
    Find highest and lowest y for the plot??? (should I or not)
    Those are limits for the plot
    Create step for the plot
    Evaluate expression1 and expression2 for each x
    Show the plot
     */
}